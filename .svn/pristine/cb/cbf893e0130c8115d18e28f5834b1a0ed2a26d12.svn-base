\name{getIterationSimilarModeStates}
\alias{getIterationSimilarModeStates}
\title{Get iteration similar to mode of multi-state chains}
\usage{
  getIterationSimilarModeStates(jagsResults, data = NULL)
}
\arguments{
  \item{jagsResults}{An \code{\link[=rjags-class]{rjags}}
  result}

  \item{data}{(optional) The data used to fit the
  \code{jagsResults} containing a \code{T.POSIX} column.}
}
\value{
  Return a vector (or data frame including time) of the
  states from the iteration most closely matching the mode
  states.
}
\description{
  Get the assigned states from the iteration from the chain
  most similar to the mode state at each step. If the data
  used to fit the JAGS model is provided (a data frame
  generated by \code{GetVT} in
  \code{\link[bcpa:bcpa-package]{bcpa}}), then a data frame
  containing the state and time for each step in the track
  is returned. Otherwise, just the vector of states is
  returned.
}
\author{
  Chloe Bracis
}
\seealso{
  \link{getModeStates}
}

