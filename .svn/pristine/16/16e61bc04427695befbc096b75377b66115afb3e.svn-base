\name{getStateProbabilities}
\alias{getStateProbabilities}
\title{State probabilities}
\usage{
  getStateProbabilities(jagsResults, data = NULL)
}
\arguments{
  \item{jagsResults}{An \code{\link[=rjags-class]{rjags}}
  result}

  \item{data}{(optional) The data used to fit the
  \code{jagsResults} containing a \code{T.POSIX} column.}
}
\value{
  Return matrix of (or data frame including time) state
  probabilities for each step
}
\description{
  Get the probability of being in each state for
  multi-state chains for every step in the track. If the
  data used to fit the JAGS model is provided (a data frame
  generated by \code{GetVT} in
  \code{\link[bcpa:bcpa-package]{bcpa}}), then a data frame
  containing the state probabilities and time for each step
  in the track is returned. Otherwise, just the matrix of
  states is returned.
}

