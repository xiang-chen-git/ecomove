\name{plot.multipath}
\alias{plot.multipath}
\title{Plot multipath}
\source{
plot.multiplot.r
}
\usage{
plot.multipath(multipath, cols = NULL, ...)
}
\arguments{
  \item{multipath}{a multipath class object (minimally
  containing a vector Z of locations and a Phase element
  which enumerates the phases of the plots).}

  \item{{pch,cex}}{character type and size - defaults to
  1/2 size (Cex = 0.5) filled circles (pch=19)}

  \item{{cols}}{colors of the distinct phases.  Defaults to
  the well contrasted rich.colors() palette from gplots.}

  \item{...}{additional arguments passed to plot function}
}
\description{
Default method for plotting multipaths
}

