\name{getMeanStates}
\alias{getMeanStates}
\alias{getMedianStates}
\alias{getModeStates}
\title{Get Mean / Mode / Median State}
\usage{
  getMeanStates(jagsResults, data = NULL)

  getMedianStates(jagsResults, data = NULL)

  getModeStates(jagsResults, data = NULL)
}
\arguments{
  \item{jagsResults}{An \code{\link[=rjags-class]{rjags}}
  result}

  \item{data}{(optional) The data used to fit the
  \code{jagsResults} containing a \code{T.POSIX} column.}
}
\value{
  Return vector of (or data frame including time)
  respective mean, median or mode of states for each step
}
\description{
  Get Mean / Mode / Median state of multi-state chains.
  Note that for 3 state models, the mode is preferred, as
  the mean and median for a split between states 1 and 3
  would be state 2. If the data used to fit the JAGS model
  is provided (a data frame generated by \code{GetVT} in
  \code{\link[bcpa:bcpa-package]{bcpa}}), then a data frame
  containing the state and time for each step in the track
  is returned. Otherwise, just the vector of states is
  returned.
}
\author{
  Chloe Bracis
}

